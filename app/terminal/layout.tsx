/**
 * Terminal Page Layout
 * Excludes footer for immersive terminal experience
 */
export default function TerminalLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}

